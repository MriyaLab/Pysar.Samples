<?xml version="1.0" encoding="utf-8"?>
<!--
  TEMPORARY - delete once Pysar.Uno is republished against stable Uno.
  Adds a locally packed Pysar (built from the probe/uno6-compat branch) so the Uno sample can be
  verified before anything is published.
-->
<configuration>
  <packageSources>
    <add key="pysar-local" value="/tmp/localfeed" />
  </packageSources>
</configuration>
