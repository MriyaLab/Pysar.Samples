<Window x:Class="Pysar.Wpf.Sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:pysar="clr-namespace:Pysar.Wpf;assembly=Pysar.Wpf"
        mc:Ignorable="d"
        Title="Pysar.Wpf.Sample"
        Width="1000"
        Height="700"
        Background="#262626">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="8"
                    Orientation="Horizontal">
            <ComboBox Width="140"
                      Margin="0,0,8,0"
                      DisplayMemberPath="Title"
                      ItemsSource="{Binding Reports}"
                      SelectedItem="{Binding SelectedReport}" />

            <TextBox Width="52"
                     Margin="0,0,8,0"
                     Text="{Binding CurrentPageText, UpdateSourceTrigger=PropertyChanged}"
                     TextAlignment="Center" />

            <TextBlock Margin="0,0,8,0"
                       VerticalAlignment="Center"
                       Foreground="White"
                       Text="{Binding PageCount, StringFormat='/ {0}'}" />

            <Button Width="36"
                    Margin="0,0,8,0"
                    Command="{Binding ZoomOutCommand}"
                    Content="−" />

            <Button Width="64"
                    Margin="0,0,8,0"
                    Command="{Binding ActualSizeCommand}"
                    Content="{Binding ZoomText}" />

            <Button Width="36"
                    Margin="0,0,8,0"
                    Command="{Binding ZoomInCommand}"
                    Content="+" />

            <Button Margin="0,0,8,0"
                    Command="{Binding ToggleFitCommand}"
                    Content="{Binding FitButtonText}" />

            <Button Command="{Binding PrintCommand}" Content="Print" />
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Margin="8,0"
                   Foreground="#C0392B"
                   Text="{Binding ErrorMessage}">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <pysar:ReportView x:Name="Viewer"
                            Grid.Row="2"
                            Background="#292929"
                            CurrentPage="{Binding CurrentPage}"
                            DocumentPadding="16"
                            EffectiveZoom="{Binding EffectiveZoom, Mode=OneWayToSource}"
                            PageBorderColor="#3E4351"
                            PageBorderThickness="2"
                            PageCount="{Binding PageCount, Mode=OneWayToSource}"
                            PageSpacing="16"
                            RenderBudget="256"
                            RenderFailed="OnRenderFailed"
                            Report="{Binding Report}"
                            Zoom="{Binding Zoom}"
                            ZoomMode="{Binding ZoomMode}" />
    </Grid>
</Window>
