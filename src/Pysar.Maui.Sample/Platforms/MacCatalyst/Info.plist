<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- The Mac App Store requires you specify if the app uses encryption. -->
    <!-- Please consult https://developer.apple.com/documentation/bundleresources/information_property_list/itsappusesnonexemptencryption -->
    <!-- <key>ITSAppUsesNonExemptEncryption</key> -->
    <!-- Please indicate <true/> or <false/> here. -->

    <!-- Specify the category for your app here. -->
    <!-- Please consult https://developer.apple.com/documentation/bundleresources/information_property_list/lsapplicationcategorytype -->
    <!-- <key>LSApplicationCategoryType</key> -->
    <!-- <string>public.app-category.YOUR-CATEGORY-HERE</string> -->
	<!--
		2 is "Scaled to Match iPad", the mode .NET MAUI targets. The Mac-optimised value, 6, lays
		the interface out in macOS points, but Mac idiom rejects UIKit controls that MAUI relies on -
		Picker is backed by a UIPickerView, which throws the moment it is shown.
	-->
	<key>UIDeviceFamily</key>
	<array>
		<integer>2</integer>
	</array>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.lifestyle</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<!--
		The icon deliberately does not go through XSAppIconAssets. An asset-catalog icon lands in
		the bundle as CFBundleIconName, and macOS renders that as an "iOS app on Mac" icon: the
		artwork is shrunk onto a light rounded plate, so the icon reads as light grey rather than
		black. A classic .icns named by CFBundleIconFile is rendered as-is.
		AppIcon.icns is generated from the same SVGs by tools/generate-mac-appicon.swift.
	-->
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
</dict>
</plist>
